# Setup
1. Download google-services.json from Firebase Console and place at android/app/
2. Run `flutterfire configure` to regenerate lib/firebase_options.dart
3. Run `flutter pub get`
4. Run `flutter run`